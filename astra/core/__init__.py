"""Astra core: shared core subsystems (config, events, tasks, permissions,
orchestrator, ...) live in sibling modules of this package: astra.core.*.

NOTE: the Plugin interface and Registry class that used to live here have
been removed. `plugins/` is an empty placeholder for future plugins (see
plugins/README.md).
"""
from __future__ import annotations
