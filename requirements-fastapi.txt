# Optional FastAPI/ASGI server for Astra (astra/web_fastapi.py + run_fastapi.py).
#
# The default server needs NOTHING installed:  python3 run.py
# Install this extra only if you want the ASGI variant:  python3 run_fastapi.py
#
#   pip install -r requirements-fastapi.txt
#
# Deliberately absent:
#   python-multipart — uploads are parsed by the shared stdlib parser in
#   astra/web_core.py, so the two servers handle multipart identically.
#
# Termux/Android: pydantic 2 ships a compiled Rust core and publishes no
# Android wheel, so pip builds it from source. Install the toolchain first
# (pkg install rust binutils) and expect the first install to take a while.
fastapi>=0.110
uvicorn>=0.27
