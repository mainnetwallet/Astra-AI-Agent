"""Package marker for the Astra test suite.

The shared harness lives in `tests/helpers.py` (import it as
`from tests.helpers import make_stack, LiveServer, ...`). The plugin system
has been removed — `plugins/` is an empty placeholder (see
`plugins/README.md`), so the legacy `plugins` list is always empty.
"""
